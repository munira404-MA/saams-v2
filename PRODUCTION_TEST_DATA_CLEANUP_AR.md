# تنظيف بيانات الاختبار قبل التشغيل الرسمي

تم تجهيز مرحلتين آمنتين:

1. `supabase/database_v1_0/08_TEST_DATA_CLEANUP_PREVIEW_READ_ONLY.sql`
   - قراءة فقط.
   - لا يحذف ولا يغير أي بيانات.
   - يعرض الفواتير والسلف والتوزيعات وسجل الحركات الموجودة حاليًا.

2. `supabase/database_v1_0/09_CLEAN_TEST_TRANSACTION_DATA_RUN_AFTER_PREVIEW.sql`
   - ملف حذف فعلي.
   - لا يُشغّل قبل مراجعة نتيجة الملف 08.
   - يحذف فقط: الفواتير، توزيعات السلف، السلف، وسجل الحركات التجريبي.
   - يحافظ على: الحضانات، الحسابات، الصلاحيات، وهيكل النظام، ولا يلمس الأصول.

## الخطوة الحالية
شغلي الملف 08 فقط في Supabase SQL Editor، ثم راجعي النتائج قبل تشغيل الملف 09.
